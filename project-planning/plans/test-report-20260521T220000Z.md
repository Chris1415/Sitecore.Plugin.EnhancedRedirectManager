# Test Report -- PRD-004 (Regex Source Mode + Test Surface)

---
document_type: test_report
artifact_name: test-report-20260521T220000Z.md
generated_at: 2026-05-21T22:00:00Z
agent: 07-qa-specialist
prd_id: PRD-004
run_manifest: project-planning/workflow/current-run.json
phase: 1 (automated + manual verification)
status: tested_pending_smoke
---

## 1. Automated Checks

| Check | Command | Result | Detail |
|-------|---------|--------|--------|
| Lint | cd site && npm run lint | PASS (baseline match) | 10 errors / 44 warnings. All 10 errors in gitignored _upstream-source.ts staging file. 0 new errors vs pre-PRD-004 baseline. Warnings delta: -4 vs pre-code-review baseline (was 48 after T4; 44 after code-review minor fixes). |
| Build | cd site && npm run build | PASS | Clean Next 16.1.7 Turbopack build; 9 routes; 0 TypeScript errors; static + dynamic routing correct. |
| Unit / UI (Vitest) | cd site && npm test | PASS -- 691/691 | 76 test files, 691 tests, 0 failures, 0 skipped. Duration: 17.72s. Includes 3 regression tests added by code-review (M1: 2 tests in proxy-simulator.test.ts; M2: 1 test in ResultCard.test.tsx). |
| Skipped tests | grep .skip across suite | CLEAN | Zero .skip() / it.skip / test.skip / describe.skip directives. One match was a data assertion on totals.skipped -- not a skip directive. |
| SDK-contract greps | client.mutate / .data.data / empty catch | CLEAN | 0 hits across all PRD-004 scope. proxy-simulator.ts makes no SDK calls. |

## 2. Manual Verification -- Section 8 Coverage Cross-Reference

### US-R1 -- EditRowModal mode toggle (Pattern / Regex)

| Behavior | Covered by | Verdict |
|----------|------------|---------|
| Mode toggle defaults to Pattern on every open (FR-A2 / ADR-0040) | EditRowModal.test.tsx | GREEN |
| Switching mode preserves source and target edits | EditRowModal.test.tsx | GREEN |
| Save-time validation: invalid regex blocks save with inline error (M1 / AC-R1.5) | EditRowModal.test.tsx -- save-time validation test block | GREEN |
| Save-time validation: $0 reference rejected | EditRowModal.test.tsx | GREEN |
| Save-time validation: $N > group count rejected | EditRowModal.test.tsx | GREEN |
| Save-time validation: $siteLang exempt from group-count cross-check | EditRowModal.test.tsx | GREEN |
| Save-time validation: Pattern mode + regex meta-chars -> non-blocking hint only (AC-R1.6) | EditRowModal.test.tsx | GREEN |
| Cancel with unsaved changes -> confirm dialog | EditRowModal.test.tsx | GREEN |
| Cancel without changes -> closes immediately | EditRowModal.test.tsx | GREEN |
| Save invokes updateRedirectMap with correctly mutated mappings array | EditRowModal.test.tsx | GREEN |
| EditRowModal structural guard: no RedirectType / IncludeVirtualFolder / PreserveQueryString / PreserveLanguage (ADR-0043 / FR-A8) | tests/structural/structural-guards.test.ts | GREEN (11/11 structural guards) |

### US-R2 -- Inline mode-mismatch hint (REMOVED per 2026-05-21 simplification)

| Check | Verdict |
|-------|---------|
| EditRowModal.tsx source contains NO hint-rendering JSX | CONFIRMED -- file header comment lists inline hint under Removed in 2026-05-21 simplification. 0 hits for role=status in source. |
| EditRowModal.test.tsx removed corresponding T024 tests | CONFIRMED -- test file header: Removed tests for T020 snippet library, T021 chips, T022 sample URL tester, T024 inline hint. |
| Snippet library absent from modal | CONFIRMED -- 0 hits for snippet / REGEX_SNIPPETS in EditRowModal.tsx non-comment source. |
| Capture-group chips absent from modal | CONFIRMED -- 0 hits for chip in EditRowModal.tsx non-comment source. |
| Sample URL tester absent from modal | CONFIRMED -- 0 hits for sampleTest in EditRowModal.tsx non-comment source. |
| No orphan test files asserting on removed components | CONFIRMED -- 691/691 GREEN confirms no orphan specs broke. |

### US-T0 -- Shared rail (maps-as-prop per 2026-05-21 simplification; ScopePicker deferred as FO-9)

| Behavior | Covered by | Verdict |
|----------|------------|---------|
| Maps passed from FullPage.maps state to TestSurface (C9 -- no ScopePicker in TestSurface) | FullPage.tsx line 394; TestSurface.test.tsx confirms no inline maps list | GREEN |
| CollectionPicker + SitePicker shared via buildRailContent (Manage + Test see same scope) | Operator Level 1 walkthrough confirmed; FullPage.tab.test.tsx covers tab state preservation | GREEN |
| URL input accepts absolute + path+query forms | TestSurface.test.tsx -- deriveLocaleFromUrl tests (6 cases), URL validation test block | GREEN |
| Locale derived from URL prefix via LOCALE_PREFIX_REGEX; defaults to en (C6 -- no locale dropdown) | TestSurface.test.tsx -- simulate() is called with locale derived from URL prefix | GREEN |

### US-T1 -- Test surface URL -> trace

| Behavior | Covered by | Verdict |
|----------|------------|---------|
| URL input validation: inline error for non-http non-/ start | TestSurface.test.tsx -- shows URL error when value does not start with http or / | GREEN |
| Test button disabled when URL empty | TestSurface.test.tsx -- Test button is disabled when URL is empty | GREEN |
| Test button disabled when URL invalid | TestSurface.test.tsx -- Test button is disabled when URL is invalid | GREEN |
| Test button enabled when valid URL entered | TestSurface.test.tsx -- Test button is enabled when valid URL is entered | GREEN |
| Test button click -> simulate() called with flattened rules from maps | TestSurface.test.tsx -- simulate call test block | GREEN |
| Trace rendered in right column after onTraceComplete | TestSurface.test.tsx -- calls onTraceComplete with the simulation result | GREEN |
| TraceCardStack renders all SimulationStage.kind values without error | TraceCardStack.test.tsx -- exhaustive switch coverage | GREEN |

### US-T2 -- Diagnose non-match (rows-considered card load-more behavior, M2 from code-review)

| Behavior | Covered by | Verdict |
|----------|------------|---------|
| ResultCard unmatched: shows first 5 rows by default | ResultCard.test.tsx -- shows first 5 rows when there are more than 5 | GREEN |
| Expand button shows when rows > 5 | ResultCard.test.tsx -- shows more rows considered expand button when rows > 5 | GREEN |
| Expand reveals more rows | ResultCard.test.tsx -- expands to show more rows when expand button is clicked | GREEN |
| M2 regression: load-more button disappears after all rows revealed (hiddenCount = rows.length - showCount) | ResultCard.test.tsx -- load-more button disappears after all rows are revealed (regression: infinite loop fix M2) | GREEN |
| M2 fix in ResultCard.tsx source: hiddenCount = rows.length - showCount | Source confirmed at line 141 with explanatory comment citing code-review M2 | CONFIRMED |

### US-T3 -- Simulator vs upstream parity (M2)

| Behavior | Covered by | Verdict |
|----------|------------|---------|
| All 15 upstream fixture cases GREEN | proxy-simulator.test.ts -- upstream fixture parity describe block over upstream-cases.json (count: 15, SHA 30b0db8fe768b83f03fd6b9772b0d3e14711c6b2) | GREEN |
| All 8 tenant fixture cases GREEN | proxy-simulator.test.ts -- tenant fixture cases describe block over tenant-cases.json (count: 8) | GREEN |
| Known divergences from upstream: empty (100% parity per M2) | proxy-simulator.ts header Known divergences list confirmed empty | CONFIRMED |

### ADR-0039 -- Regex safety (100ms per-row + 3s wall-clock cap)

| Behavior | Covered by | Verdict |
|----------|------------|---------|
| runTimedTest returns match / no-match / timeout -- never throws | proxy-simulator.test.ts -- runTimedTest describe block (3 cases) | GREEN |
| Wall-clock cap test uses Date.now() mock (ADR-0039 amendment 2026-05-20T21:30: synchronous regex.test() cannot be interrupted by Promise.race in single-threaded JS) | proxy-simulator.test.ts -- wall-clock-cap describe block; Date.now spy documented with rationale | GREEN (behavioral contract) |
| M1 fix: REGEXP_CONTEXT_SITE_LANG.lastIndex = 0 before each .test() call | proxy-simulator.ts line 573 confirmed; also regex.lastIndex = 0 at lines 495, 511, 536 | CONFIRMED |
| M1 regression: consecutive simulate() calls on same $siteLang input produce identical results | proxy-simulator.test.ts -- REGEXP_CONTEXT_SITE_LANG stateful regex regression (code-review M1) block (2 tests) | GREEN |

### Deep-link Test -> Manage -> EditRowModal pre-focused (ADR-0041 / FullPage.deeplink.test.tsx)

| Behavior | Covered by | Verdict |
|----------|------------|---------|
| handleRequestEditRow switches to Manage tab when map found | FullPage.deeplink.test.tsx -- handleRequestEditRow: switches to Manage tab when map IS found | GREEN |
| handleRequestEditRow: map not found -> stays on Test tab (guard branch, explicit toast) | FullPage.deeplink.test.tsx -- when map is not found, stays on Test tab (guard branch) | GREEN |
| onRequestEditRow callback passed to TestSurface | FullPage.deeplink.test.tsx -- passes onRequestEditRow callback to TestSurface (T039) | GREEN |
| ResultCard matched: true -> Open this rule in Manage invokes onRowClick(mapId, rowIndex) | ResultCard.test.tsx -- calls onRowClick with mapId and rowIndex | GREEN |

### Regression -- Pre-PRD-004 behaviors

| Behavior | Verdict |
|----------|---------|
| Manage tab CRUD: + New map, edit existing map, delete map | GREEN -- pre-PRD-004 tests pass in 691/691 suite |
| Context Panel | GREEN |
| Dashboard Widget | GREEN |
| Theme toggle | GREEN |
| Export / import flows | GREEN |
| Existing Authoring GraphQL helpers (listSites, listCollections, redirects-read, redirects-write) called with byte-identical shapes | GREEN -- SDK-contract greps clean; structural guards confirm no new .mutate / .data.data calls |

## 3. Visual Testing

| Surface | Method | Verdict | Notes |
|---------|--------|---------|-------|
| Host-frame Playwright smoke (5-axis vs POC at pocs/poc-v1-prd004/) | Deferred | WARN -- deferred: host URL not supplied | Per framework rule (sitecore:marketplace-sdk-host-frame-testing): host URL and Cloud Portal auth must be operator-supplied. No host URL was provided in this session. Full 5-axis Playwright pass (light / dark / system / reduced-motion / mobile <768px) remains outstanding. Named deferred item, not a silent skip. |
| Live operator walkthrough | Operator Level 1 (prior to /test) | pass_with_caveats | Exercised: (1) URL-mode rules -- passes; (2) Regex-mode rules with capture-group substitution -- passes; (3) Save-time validation (invalid regex blocks save; capture-group cross-check) -- passes; (4) Cheatsheet card in Regex mode -- visually accepted; (5) Shared CollectionPicker + SitePicker across Manage/Test tabs -- scope inheritance works; (6) RedirectMapList readonly in Test tab -- no click handler fires; (7) TestSurface bug (Test button race + null-safety crash) -- found and fixed during operator smoke. Caveat: T6 real-tenant URL-prediction smoke still pending -- 5-10 URL formal pass required before status can advance from shipped_with_caveats to shipped. |
| Mobile responsive (FR-B2) | N/A inside iframe | Not applicable | TestSurface two-column grid stacks at <768px per Tailwind grid-cols-[360px_1fr] md: breakpoint. Confirmed in TestSurface source. Full visual confirmation requires host-frame Playwright smoke (deferred above). |

## 4. Findings

### Critical

None.

### Major

None. Both Major findings (M1 REGEXP_CONTEXT_SITE_LANG race; M2 ResultCard infinite load-more) were identified and auto-fixed during code review. Fixes verified in source (lines 573 and 141 respectively). Regression tests GREEN.

### Minor

| # | Finding | Severity | Status | Location |
|---|---------|----------|--------|----------|
| m5 | EmptyState copy references "on the left" -- slightly imprecise after 2026-05-21 simplification; picker is in the shared rail, not a panel-specific left side | Minor | Operator triage -- no auto-fix applied. Cosmetic, not a UX blocker. Operator may revise to "Select a collection and site in the left rail." | site/components/full-page/EmptyState.tsx:50 |

This finding was identified in the code review and carried forward as operator-triage. No new findings identified in this test pass.

## 5. Code-Review Fixes Verification

| # | Finding | Fix | Verification |
|---|---------|-----|--------------|
| M1 | REGEXP_CONTEXT_SITE_LANG stateful g-flag race | proxy-simulator.ts:573: REGEXP_CONTEXT_SITE_LANG.lastIndex = 0 before .test() | Source confirmed at line 573; also regex.lastIndex = 0 at lines 495, 511, 536 for candidate loops. M1 regression describe block: 2 GREEN tests. |
| M2 | ResultCard hiddenCount vs rowsConsideredTotal infinite loop | ResultCard.tsx:141: hiddenCount = rows.length - showCount | Source confirmed at line 141 with comment citing M2. Regression test GREEN. |
| m1 | setVisible in effect lint error | use-staggered-render.ts:56: targeted eslint-disable-next-line | Lint: 44 warnings (down from 48 before code-review); no new errors. |
| m2 | Unused HoverLiftCard import in TraceCard.tsx | Import removed | 0 hits for HoverLiftCard in TraceCard.tsx. |
| m3 | Unused onOpenChange destructure in outer EditRowModal wrapper | Removed from outer component destructure | Warning gone in 691/691 GREEN run. |
| m4a | Unused waitFor import in TraceCardStack.test.tsx | Removed | Clean. |
| m4b | Unused ITEMS_PQ constant in use-staggered-render.test.tsx | Removed | Clean. |

## 6. Release Readiness Assessment

| Gate | Status |
|------|--------|
| M1 -- 100% malformed regex caught at save | PASS -- unit tests GREEN |
| M2 -- 100% upstream fixture parity | PASS -- 15/15 upstream cases GREEN, 0 known divergences |
| M3 -- T6 real-tenant smoke >= 80% URL match | PENDING -- operator-driven 5-10 URL walkthrough on Full Page Test tab required |
| M4 -- Operator can diagnose non-match in <30s | Pending T6 operator walkthrough (trace structural coverage GREEN in unit tests) |
| NFR-1 -- Test click to trace render p95 <= 200ms for <= 100-rule inventories | Unit tests cover correctness; p95 timing unmeasured without host-frame run |
| NFR-6 -- Net bundle <= 25KB gzipped | Build clean; no runtime regex-parser dependency; formal measurement pending host-frame run |
| Build clean | PASS |
| Lint -- no new errors vs pre-PRD-004 baseline | PASS -- 10 errors all pre-existing in gitignored staging file |
| 691/691 tests GREEN | PASS |
| Code-review Critical/Major fixes resolved | PASS -- all verified in source |
| Host-frame Playwright smoke (5-axis vs POC) | WARN -- deferred: host URL not supplied |

**Overall status: tested_pending_smoke**

The automated test suite is fully GREEN. The implementation is behaviorally correct per unit and component test contracts. Two pending items block advancement to tested or shipped:

1. **T6 real-tenant smoke (M3 ship gate):** Operator must run 5-10 known production URLs through the Full Page Test tab, compare simulator output vs tenant Edge response. >= 80% match -> shipped; < 80% -> shipped_with_caveats; parity bug -> hard-stop.
2. **Host-frame Playwright smoke (NFR-3 / global theme policy):** 5-axis visual diff vs POC requires operator-supplied Cloud Portal host URL and interactive auth. Recorded as WARN -- deferred.

The minor EmptyState copy item (m5) is cosmetic and does not block ship.

No Phase 2 (Developer fix cycle) required -- zero new Critical or Major findings in this test pass.