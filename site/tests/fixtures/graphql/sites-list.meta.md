# sites-list.json — fixture metadata

```
CAPTURE STATUS: assumed
SOURCE: marketplace-sdk-xmc skill snapshot 2026-05-10 — Sites.Site type from
        node_modules/@sitecore-marketplace-sdk/xmc/dist/xmc/src/client-sites/types.gen.d.ts (line ~964)
DIVERGENCE-DETECTION: T065 real-tenant CRUD smoke captures the actual shape; if it differs,
        replace sites-list.json and the divergence-detection RED test will fail with a clear delta.
RESPONSE ENVELOPE: { data: { data: Array<Sites.Site> } } — DOUBLE .data.data unwrap (xmc module query)
```
